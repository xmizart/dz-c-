internal class Program
{
    private static void Main(string[] args)
    {
        int playerDamage;
        int playerArmor;
        int playerHealth;
        int enemyHealth
        int enemyArmor
        int enemyDamage
        float levelProgress;
        string playerName;
        string characterDescription;
        bool isPlayerAlive;   
    }
}
